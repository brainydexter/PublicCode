#ifndef _UTILS_H_
#define _UTILS_H_

#include <string>

#include "defines.h"

std::string convertBoolToString(bool value);
std::string convertStatusToString(const STATUS& status);

#endif // _UTILS_H_